// EXERCÍCIO: Crie a função 'swap' que, dado um array de 3 elementos, retorna um novo array com o 1° e 3° trocados.
// A função deve ter uma única linha.

const myList = [1, 2, 3];

// escreva swap abaixo
const swap = ([a, b, c]) => [c, b, a];

console.log(swap(myList));
